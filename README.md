# Task Manager App

A minimal Angular application scaffolded manually for the `TaskManagerApp` workspace.

## Setup

```bash
npm install
npm start
```

## Available scripts

- `npm start` - runs the app in development mode
- `npm run build` - builds the app for production
- `npm test` - runs unit tests
